# Run Instructions 

## Ubuntu

1. Clone the project
2. Edit your on system ansible hosts file to add/change the IP and host(if required).
3. Add the value of the host in the main.yml
4. Test the DB connection of the slave server using mysql command.
5. Run the create.sh script
6. Run the playbook. 
